{
  "name": "subx",
  "version": "0.9.7",
  "description": "SubX, Next generation state container",
  "repository": "git@github.com:tylerlong/subx.git",
  "author": "Tyler Long <tyler4long@gmail.com>",
  "license": "MIT",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "keywords": [
    "SubX",
    "Sub X",
    "Reactive Subject",
    "State Container"
  ],
  "scripts": {
    "bench": "ts-node benchmark/find.ts",
    "test": "jest -w 1",
    "prepublishOnly": "yarn test && yarn compile",
    "compile": "rm -rf build/* && tsc && mv build/src/* build/ && rmdir build/src && rm -rf build/test",
    "pretest": "yarn run compile"
  },
  "dependencies": {
    "ramda": "^0.27.0",
    "rxjs": "^6.5.5"
  },
  "devDependencies": {
    "@types/benchmark": "^1.0.33",
    "@types/jest": "^26.0.0",
    "@types/node": "^14.0.13",
    "@types/ramda": "^0.27.6",
    "benchmark": "^2.1.4",
    "gts": "^2.0.2",
    "hyperid": "^2.0.5",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5",
    "wait-for-async": "^0.5.0",
    "yarn-upgrade-all": "^0.5.2"
  },
  "files": [
    "build/*",
    "src/*",
    "yarn.lock"
  ]
}
